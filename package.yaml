name:                02d
version:             0.1.0.0

dependencies:
# lts/20/2.yaml
- base == 4.18.2.1

# https://www.stackage.org/lts-22.43
# 4.18.2.1


ghc-options:
- -threaded
- -O2
- -Wall

library:
  source-dirs:      src

tests:
  02d-test:
    main:                Spec.hs
    source-dirs:         test
    ghc-options:
    - -threaded
    - -O2
    - -rtsopts
    - -with-rtsopts=-N
    dependencies:
    - 02d
    - hspec    
